# STATE-2026-08-26 — the reconciliation: every claim re-verified, the strands rejoined

Successor to STATE-2026-08-25.md. That ledger graded the board from origin at
f508223 and marked the Aug-25 local work CLAIMED. This audit re-verified
everything — the CLAIMED items, the merged branches, and every repair the
Aug-20/Aug-25 reviews demanded — against the code at origin/design/layers-core
41da919, by reading the mechanisms, not the commit messages. It then merged
the one stranded organ. Every VERIFIED line below was checked in this
container: 90/91 golden suites green on the merged tree (the one red is
audio.mjs shelling out to yt-dlp, absent here — environmental on every
branch), tsc clean.

## 0. What changed on the board since STATE-2026-08-25

- **Item 0-A (the unpushed five days) is CLOSED.** origin/design/layers-core
  now carries b321b90 (コマ送り six repairs), b2b408c (four audit defects +
  the hint floor), 9966e26 + 41da919 (§29 rungs 0–1). All verified below.
- **The 問い organ was stranded and is now merged.** d018752 (standing
  questions) + STATE-2026-08-25.md lived only on
  claude/apple-calendar-video-analysis-014lv7, which forked at f508223 and
  never saw the four newer commits. The same night, the local session wrote
  問い's spec into DESIGN §29 rung 6 and its commit messages said standing
  questions "remain unbuilt" — **two sessions built spec and mechanism of the
  same organ in parallel, each blind to the other.** This branch
  (claude/jpp-collocations-analysis-7600kg) is layers-core + that merge:
  every source file auto-merged, main.js rebuilt from merged sources, sweep
  54/54 and x-relevance 30/30 both green together. Fast-forward
  design/layers-core to adopt; delete this branch to reject.
- **Item 0-B (the front door) is STILL OPEN.** origin HEAD is still `main`
  (the March/Copilot world — the repo README, dist/ and release workflow all
  point at code frozen five months ago). The
  claude/tategaki-mobile-version-k7hww8 branch still builds a vertical
  reader on that stale main, while コマ送り records 縦書き as a refusal —
  a decision the user has not ratified in DESIGN either way.

## 1. VERIFIED — the Aug-20 review's seven repairs, re-checked at 41da919

All seven hold in code (not trusted from f508223's message):

1. Gravity landing carries scene as `card.said`, never `text\nsentence`
   (main.ts landHeldChip, with the corruption trace as its comment).
2. Hold dock at `z-index: var(--layer-popover, 40)` (styles.css).
3. Reachability debt is 8 and TRUE: `D:surface-hotkeys` restored with the
   deleted-line history written into the suite header; 50 checks pass.
4. crossSentence is ONE predicate (`notationCrossesSentence`, pipeline.ts)
   shared by CaptureModal save and capture-bundle store; the declaration is
   the parenthesized form ONLY — a bare 。 splits but declares nothing (the
   pasted-two-sentence accident is closed); （。） full-width recognized;
   ～ U+FF5E in BOTH split alphabets (the third-alphabet hole closed).
5. matchLink honors the mint contract: any ender counts as the declared
   boundary (a ！-minted link is no longer stored-and-inert), and
   clause-scale is ENFORCED — at most one ender per gap — instead of
   asserted. The two-line-window truth is documented at the call site.
6. seenT dedup: untimestamped lines key by index (`t ?? \`i${i}\``) — the
   one-sighting-per-tweet/note/article cap is dead. The old comment that
   cited the bug as proof of safety is replaced by the history.
7. The five lying docstrings corrected — hold-dock states invariant-9 status
   honestly (2 of 4 verbs have twins, the rest wait on the film), hold.ts
   scene law now describes the `said` road, sweep-match describes the
   two-line window.

## 2. VERIFIED — the コマ送り six repairs (b321b90), each mechanism read

1. 辞書 space-AND with the empty state naming the FAILING term
   (`queryTerms`/`missMessage`, both live-search and sidecar paths).
2. Mid-word selection grows back through its sentence before any
   deinflection guess is trusted (`lookUpGrown`, main.ts ~4066 — the
   まない→まる phantom is dead; exact hits still win outright).
3. One selection, one airspace — the echo yields ABOVE to the native menu
   and takes BELOW when a slate touch/pen selection makes the native bar
   likely (selection-echo.ts ~157).
4. The drop road never executes a guess — mismatched payloads repaint the
   rack as a chooser; nothing runs until the hand picks; Escape cancels
   (drop-router.ts armChooser).
5. `ui/keyboard-aware.ts`, armed once in view-chrome so every surface gets
   it: docked/split keyboard inset shortens the pane; the floating 10-key is
   honestly named unseeable, focused input scrolled into view.
6. Carry commits on MOTION, never on time (pointer-drag.ts): ARM_MS is GONE
   from the codebase; the deadline matures into a silent `--held` lift;
   move = carry from the nib's current position; release-in-place = the
   row's ordinary tap. CLAUDE.md invariant 18. Note for the record: this
   design is robust to the hover-vs-contact ambiguity the Aug-25 critique
   raised against the parked-tip law — if the filmed parks were hovers the
   mechanism costs nothing, and if they were contacts it prevents the
   misfire; the repair is sound EVEN IF the measurement wasn't.

Plus verified: 𝕏 `patternCache`/`usageCache` memoization, manifest 1.0.1,
PHYSICS-2026-08-19.md and CALENDAR-PHYSICS-2026-08-24.md in claude/.

## 3. VERIFIED — b2b408c (four audit defects + the exposed fifth) and §29 rungs 0–1

- Usage badges scope their jump per book block; suggestWhy re-answers for
  the selected class; gloss rides L0 only; the 語法プロフィール row road
  into 🔵 連語 carries the pair as the capture; HINT_FLOOR = 4 in
  `chooseSuggested` (pure, golden-pinned).
- §29 rung 0: `src/x/relevance.ts` — pure, oracle injected; 足して's 23
  false friends demoted to a NAMED 部分一致 tail, never deleted. Rung 1:
  `rankByClass`, one comparator per class, each score carrying its stated
  reason; sort chips demoted to tiebreak; `probeFor` seam in main.ts. 30
  golden checks. Rungs 2–6 specified in §29.2, unbuilt — except rung 6's
  catalog-side substrate, which is the merged 問い (see §29's status line).

## 4. OPEN — the queue as it actually stands after this audit

1. **Adopt or reject this merge** (fast-forward design/layers-core, or
   delete the branch). Until then the repo again holds two truths about 問い.
2. **The front door** (0-B): re-point origin HEAD / README / releases at the
   real plugin; decide 縦書き in DESIGN (ratify the refusal or the tategaki
   branch — currently both exist and disagree).
3. **Film the walk** — the standing acceptance test (PHYSICS §6): the same
   walk, Monokakido vs plugin, side by side. IMG_1197 broke the
   never-filmed seal and its six defects are shipped; the side-by-side and
   Film Script 0 remain. Every feel claim in §2 above is code-read until
   then. pen-probe (main.ts:1614) settles the hover question in 30s.
4. **宛名札** — the address chip on every carry/flick (CALENDAR-PHYSICS
   §2.2); would also prevent the drop-chooser's mismatch case at source.
5. **In-entry Find and descend/flip tempo** — the two 答え合わせ items that
   shipped nowhere and were off every ledger until now (written into §30).
6. **鋳造 · Move-2 inspector registers · 現在線** (CALENDAR-PHYSICS §2.3,
   §2.1/2.6, §2.5).
7. **Monokakido every-session gaps, still all absent** (verified by grep,
   not assumption): land-lit arrivals from sweep/用例/chips, prev/next
   neighbour chips with instant flip, persistent dated History (today:
   session-only array, DictionaryView.ts:81), menu-carries-state
   「もう台帳にある」, Ends match mode, Example scope (shape-index already
   types 用例 — a scope, not a data gap), homophone paging, ≡ outline.
8. **The nav grammar** — the user's own correction, still living ONLY in
   chat and a claude.ai artifact, in no repo doc until this line: the axis
   tells the hand its stratum (list scrolls ↕, entry reads ↔);
   Kindle-quick paging that is scrolly-continuous, not discrete page flips;
   drag-past-end of an entry = carry me to the neighbour (the user's
   sideways drag on 唾を付ける, read as intent, not stumble); pinch-in =
   collapse to outline (the unanswered splay on 味噌). §29 rung 6's
   one-gesture filing belongs to this same hand-grammar pass.
9. **§29 rungs 2–5** — environment promotion, descent ladder + verdicts,
   the 転 table, the authority cycle ▾ (which is also the 問い's horizontal
   step — the ladder and the cycle are one structure).
10. **D:surface-hotkeys** — honest at 8; the Elecom/Logi substrate is still
    unbuilt and the ledger still says so.
11. **The films (or their window ledger) into the repo** — frame citations
    (1175 f0406…) remain uncheckable by any next session. The two PHYSICS
    docs are in; the コマ送り/答え合わせ tables, the window list with fps
    and offsets, and the source paths are not.

## 5. Coordination hazards, named so they stop recurring

- **Territory**: STATE-2026-08-25 assigned XSearchView.ts / src/x/ to a
  parallel Fable session. Rungs 0–1 were then BUILT AND PUSHED by the local
  session (9966e26, 41da919). Any session working "the xsearch view stuff"
  must fetch and read §29's status lines FIRST — the assignment in the old
  ledger is stale, and a second independent rung 0 would be exactly the
  問い/rung-6 collision repeated in the one place it's been seen coming.
- **The blind-parallel pattern**: 問い is the second time one night's work
  produced two unmerged truths (the first was the five-days-unpushed
  incident). The cheap rule: a session that ships to a side branch says so
  in the DESIGN status line it touches; a session that writes "unbuilt"
  fetches first.
- **Method, carried forward from 08-25**: VERIFIED/CLAIMED/OPEN discipline;
  goldens accrete on pure cores unless apertures are demanded per-feature;
  stumble-weighted film sampling has no denominator — record what was NOT
  sampled; and a repair derived from a disputed measurement should be
  designed (as repair 6 was) to be correct under either reading.

## 6. Built the same evening (this branch, after the audit above)

The user's directives: fast-forward, then the navigation grammar, then the
X-dict check, then "no lag, no hiccups anywhere".

- **design/layers-core fast-forwarded** to the reconciled line (問い merged);
  then the parallel session's §29 rung 3 (683b53e, src/x/probe.ts) landed
  mid-audit and was merged too — the branch holds everything again.
- **§30.1 — the 辞書 nav grammar, SHIPPED** (see DESIGN for the full spec):
  land-lit arrivals on both render halves · reading-ordered neighbour chips
  at the bottom corners with instant flip (the filmed 病人→病毒→廟堂 walk is
  golden-pinned) · kindle-quick flick paging with the axis grammar (↕ within,
  ↔ between) · pinch-in → ≡ outline · in-screen Find · persistent dated
  History under `_dictHistory` · descend-animated/flip-instant tempo with the
  per-keystroke stagger dead and pinned dead. Pure core golden/dict-nav.mjs
  25/25; apertures reachability D4 (11 checks); command twins for all five
  gestures. The two recovered 答え合わせ items are no longer open — they are
  in this build.
- **§29 verified beyond the commit claims**: relevance.ts (own-words guard,
  tightest-swallower, named tail), rankByClass (per-class comparators, why on
  every row, tiebreak demotion), probe.ts (core/floor/plateau stops,
  nearest-vs-surviving, rivals only when supplied) — mechanisms read, goldens
  re-run, integration checked at the view seam. One defect found and fixed:
  the rung-0 oracle's `isWord` ran lookup()'s deinflection fallback on every
  miss — thousands of misses per keystroke; now `hasExactSurface()`.
  Rungs 2/4/5 and rung 6's X-side remain the parallel session's lane.
- **The perf pass, measured not guessed**: trueHits worst case benchmarked
  (1,200 partial occurrences → 11.4ms in-container — under the debounce);
  the real costs were elsewhere: the 𝕏 pane's 300-card synchronous DOM
  rebuild (now chunked 60/frame under a generation guard, orphaned by every
  other painter of the pane), 語彙's undebounced full-tab refresh per
  keystroke (now 90ms), the dict stagger (dead), a leak-prone outline
  listener (instance-owned now). Checked and CLEAN: no non-passive
  touch/wheel listeners anywhere, all intervals registered/cleared, reflow
  reads are event-scoped singles, patternCache/usageCache/descentCache keys
  sound, the background pass is idle-batched with a mobile-Safari fallback.
- Suite after all of it: **92/93** (the one red is audio.mjs → yt-dlp,
  absent in this container — environmental on every branch), tsc clean.

## 7. The queue worked further, same session (§30.2 + the film ledger)

宛名札 · 鋳造 · 現在線 · もう台帳にある · the tilde match modes · homophone
paging — all SHIPPED, each with its pure core golden-pinned and its
apertures in reachability D4/D5 (68 checks now). Example scope deliberately
deferred with its reason written in §30.2. claude/FILM-LEDGER.md now holds
the fps/offset conventions, the film corpus with its two ⚠ label conflicts
stated, and the rule that a chat-only citation is a lead, not evidence.

Still open after this pass, in order: the side-by-side walk on film +
pen-probe (the user's half); the front door + 縦書き ratification (two
one-sentence decisions); Move-2 inspector registers (§2.1/§2.6 — the
stored-vs-candidate two-register discipline; a CaptureModal redesign that
deserves its own session, not a bolt-on); §29 rungs 2/4/5 + rung 6's
one-gesture filing (the parallel session's lane); the 07-15 Monokakido
benchmark, still ungridded.
