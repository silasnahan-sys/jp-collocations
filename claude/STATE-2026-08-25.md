# STATE-2026-08-25 — the whole board, audited from origin

Written by a **remote** session that can see only what is pushed. Everything
below is marked one of three ways, and the distinction is the point:

- **VERIFIED** — read in the code at `origin/design/layers-core` @ `f508223`
  (2026-08-20), the newest commit on origin at audit time.
- **CLAIMED** — reported done by the 2026-08-25 local session (six 答え合わせ
  repairs, motion-commit, 𝕏 memoization, docs). Almost certainly real — but it
  exists on ONE disk and cannot be verified from here until pushed.
- **OPEN** — verified absent from origin and not claimed by anyone.

## 0. The two urgent items, before any feature

**A. Five days of work are unpushed.** Origin's newest commit on the real
branch is `f508223` (Aug 20). Everything since lives only on the local
machine: the six 答え合わせ repairs, the motion-commit model (CLAUDE.md
invariant 18), the XSearchView memoizations, `claude/PHYSICS-2026-08-19.md`,
`claude/CALENDAR-PHYSICS-2026-08-24.md`, DESIGN §29 + §30, v1.0.1. This
project has already lost work this way twice (the three unsaved Aug-19
documents; the two unpushed commits found by the Aug-20 audit). From the
local session: `git push -u origin design/layers-core`. One command.

**B. The repository is two worlds, and the front door points at the wrong
one.** `main` is the Copilot-era plugin, untouched since 2026-03-30. The real
plugin is `design/layers-core` — the two share no history since PR #9. But
`main` owns the README install instructions, the `dist/` folder, and the
GitHub Actions release workflow — anyone (including a future session) that
follows the repo's own front door gets the five-month-old world. Also on the
wrong world: `claude/tategaki-mobile-version-k7hww8` (Aug 25) builds a
vertical reader on top of stale `main` — while コマ送り records 縦書き as a
deliberate refusal for the real plugin. Decide: retire `main`'s claim to be
the front door (point README at the real branch, or merge the real world in).

## 1. VERIFIED on origin — the Aug-20 review round is real

Every claim in `f508223` ("the Opus audit of Moves 0-1, verified and made
real") was re-checked in code by this audit. All eight hold:

| claim | receipt |
|---|---|
| 1. gravity landing carries scene as `said`, never `text\n sentence` | `main.ts:4078` comment + `pairedCard` path |
| 2. crossing = one predicate, mint→match; ≤1 ender per gap; bare 。 declares nothing | `sweep-match.ts:187-195` |
| 3. ～ U+FF5E in the join alphabet, both paren widths | `pipeline.ts:336,367` |
| 4. untimestamped lines dedup by index (the 1-per-tweet cap is dead) | `sweep-match.ts:290-307` |
| 5. hold-selection reads the editor selection first (palette works) | `main.ts:1138` |
| 6. hold dock under modals | `styles.css` `var(--layer-popover, 40)` |
| 7. ratchet restored honest: `D:surface-hotkeys`, debt **8 and true**, +10 aperture checks | `golden/reachability.mjs`, suite run: 50 pass / 8 ledger |
| 8. the five lying docstrings corrected | e.g. `sweep-match.ts:184-186` now states the two-line window |

Suite state on a fresh clone: **89/90** (the one red is `audio.mjs`, which
shells out to `yt-dlp` — environmental, not a regression). Full build clean.

Also VERIFIED as already-existing (things reports kept re-listing as gaps):
back restores scroll position (`DictionaryView.ts:293-311`), breadcrumb trail,
selection-beats-navigation guard on gloss taps (`DictionaryView.ts:~430`),
grain carried into drop verbs (`drop-intent.ts` entry branch), 願い organ
complete and watched on arrival (`reach.ts`, `main.ts autoSweepAfterCapture`),
pinch reducer exists in `input-map.ts` (what it drives still to confirm),
pen-probe command exists (`main.ts` `pen-probe`).

## 2. CLAIMED by the 2026-08-25 local session — verify after push

The six 答え合わせ repairs (tap-target probe across text-node fragments;
selection-menu ownership; 辞書 space-AND + empty state + in-entry Find; drop
rack repainted with real intents; keyboard-aware search placement; descend
tempo), the motion-commit press model replacing ARM 350 / COMMIT 700-380
timers (CLAUDE.md invariant 18), XSearchView memoization (detectPatterns per
frozen tweet, KWIC per term+corpus-size), `claude/` physics docs, DESIGN
§29 (𝕏 interrogated corpus — PROPOSED, unbuilt) and §30, v1.0.1.

**Post-push verification list** (fifteen minutes, on glass): 「ものの そうでな」
in 辞書 → narrows; tap まない inside 気が進まない → 進まない; parked Pencil on
a draggable row → nothing lifts until motion; drop a mis-previewed payload →
rack repaints, finger finishes; version reads 1.0.1.

## 3. Built by THIS session (on this branch)

**The 問い lane — standing questions** (PHYSICS §5's "cheap, load-bearing
first piece", schedulable ahead of the §29 build because its last blocker —
the null dedup key — died in `f508223`):

- `PatternEntry.standing` — an entry born BEFORE any sighting existed; the
  flag is provenance and never clears (a filled hole stays visible, §27 r3).
- `CaptureContext.standing` — the capture door files with `att: null`. The
  old path **fabricated a self-sighting** (quote = the note itself) for
  sourceless captures, so a wondering arrived looking once-encountered.
- `sweepMuted` exempts standing entries — for a question, rejection is the
  normal case (fishing); ✕ prunes a candidate (`isRejected`), never retires
  the question. Ordinary entries keep the 3+5N rule (golden-pinned both ways).
- Command `問い: File a Standing Question` beside `open-reach` — the reach
  holds a *meaning* and waits for co-presence; the 問い holds a *shape* and
  gets the full class-aware sweep on every arrival. `onSaved` already runs
  the auto-sweep, so filing the question IS asking it, immediately.
- 語彙 rows carry a neutral dashed `問` chip (class colors stay reserved);
  answers arrive through the same suggested-attestation ✓✕ road as every
  entry — no second review surface.
- Goldens: `sweep.mjs` 54/54, `patternstore.mjs` 37/37 (birth with
  `attestations: []`, answer lands + mark survives, no retro-marking of
  existing entries, round-trip).

## 4. OPEN — the reconciled queue, in order

1. **Push** (item 0A), then re-point the front door (0B).
2. **Film the plugin doing the walk** — the standing acceptance test
   ("same walk, Monokakido vs plugin, side by side") plus §2's fifteen-minute
   list. Every ergonomics claim so far is code-read or film-of-other-apps;
   the plugin's own feel is still unmeasured (pen-probe on the iPad settles
   ARM/COMMIT empirically in thirty seconds — likely moot under
   motion-commit, worth running once anyway).
3. **宛名札** — the address chip riding every carry (Calendar law: the carried
   thing is always addressed; also kills the drop-mismatch case at the
   source). Named next by the Aug-25 local session; belongs local.
4. **鋳造** — long-press mint with lane landing (Calendar's duplicate-is-a-mint;
   the hand substrate Move 3's 並べ替える needs).
5. **Move-2 inspector registers** — stored vs candidate co-visible, one commit
   at release (the strongest Calendar find); the CaptureModal-detached-from-
   its-object complaint is the same item.
6. **Monokakido every-session gaps still open on origin** (22-list #7/8/16
   and friends): land-lit arrivals (a sweep hit / 用例 / hold chip lights its
   sentence in the entry), prev/next headword chips at rest corners with
   instant flip, **persistent dated History** (`lookupHistory` is
   session-memory only), menu-carries-state (「もう台帳にある」→ the delete
   verb), Ends match mode + Example/Idiom scopes (`shape-index.ts` already
   types 用例 — the scope is small), homophone paging, ≡ entry outline.
7. **Nav corrections still un-coded**: the axis grammar (list scrolls ↕,
   entry reads ↔ — the scroll axis tells the hand which stratum it's in),
   the kindle-quick continuous entry paging, drag-past-end = carry to next
   entry, pinch-in = collapse to entry outline (half-answered by ≡).
8. **§29 / 𝕏 relevance** — owned by the parallel session building it; nobody
   else should touch `XSearchView.ts` / `src/x/` meanwhile.
9. **Current-line (現在線)**, Move 1½'s ▾ authority cycle, the Elecom surface
   hotkeys (debt entry `D:surface-hotkeys` — honest at 8).
10. **The films themselves into the repo** (or a ledger of them): today
    `_ref_monokakido/` holds six PNGs; the window/fps conventions and film
    inventory (1067/1082/1083/1144/1159/1175/1184/1212/1213/1214) exist only
    in chat transcripts. A frame citation should be checkable by the next
    session.

## 5. Standing method notes (so the next audit is cheaper)

- The gradient failure both audits named — goldens accrete on pure cores,
  zero on apertures — was answered once (reachability D2/D3). Keep answering
  it: every new gesture ships with its command (invariant 9) AND an aperture
  check where one is possible.
- "VERIFIED/CLAIMED/OPEN" discipline: a session that cannot see the vault
  must not grade feel; a session that cannot see origin must not claim
  "pushed". Both happened this week.
- Films: stumble-weighted sampling cannot answer "is it catching
  everything" — there is no denominator. Record what was NOT sampled.
